# Noctalia plugins catalog.
# Index of every plugin this source ships: the minimum needed to render, search,
# and compat-check the list. The per-plugin plugin.toml stays authoritative; the
# host re-reads it on enable. Keep one [[plugin]] row per plugin subdirectory.

[[plugin]]
id = "coldneight/lunar_calendar"
name = "Lunar Calendar"
version = "1.0.0"
updated_at = 1786205984
added_at = 1786205984
author = "coldneight"
license = "MIT"
icon = "calendar"
description = "农历日历插件：状态栏显示今日农历，点击弹出完整的农历月历（含节气、节日、干支生肖）与单日黄历详情。"
deprecated = false
plugin_api = 20
tags = ["calendar", "lunar", "chinese", "time", "utility"]
